import fbFunctions from 'metsa2020/utils/fb-functions';
import { module, test } from 'qunit';

module('Unit | Utility | fb functions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fbFunctions();
  assert.ok(result);
});
